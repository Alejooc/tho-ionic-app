import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thonline.co',
  appName: 'TuHogarOnline',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
